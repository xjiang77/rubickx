"""User-owned Micrograd R3 training exercise.

Implement ``train(seed=42) -> list[float]`` with ``micrograd.MLP`` and a
deterministic toy binary-classification dataset. Return at least 20 loss
points; the verifier requires the final loss to be at most 10% of the first.
"""

# TODO: implement train() without third-party autograd libraries.
