export class PatternError extends Error{constructor(code){super(code);this.code=code;}}
export function evaluate(input){let state="pending",value="none",error="none",terminal_count=0;const observations=[];for(const action of input.actions??[]){const op=action.op;if(["complete","fail","cancel"].includes(op)){if(state!=="pending")throw new PatternError("already_completed");terminal_count++;if(op==="complete"){state="fulfilled";value=action.value;}else if(op==="fail"){state="rejected";error=action.code;}else state="cancelled";}else if(op==="observe")observations.push(state==="pending"?"pending":state==="fulfilled"?`value:${value}`:state==="rejected"?`error:${error}`:"cancelled");else throw new PatternError("unknown_action");}return{state,observations,value,error,terminal_count};}
